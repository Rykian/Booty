const { resolve } = require('path')

module.exports = {
  config: resolve('db', 'config.json'),
  'models-path': resolve('db', 'models'),
  'seeders-path': path.resolve('db', 'seeders'),
  'migrations-path': path.resolve('db', 'migrations'),
}
